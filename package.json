{
  "name": "summer-fair-activities",
  "version": "0.1.0",
  "private": true,
  "description": "Static educational activity prototypes and Blockly migration spikes.",
  "scripts": {
    "check:blockly-theme": "node --check public/blockly-compact-theme/compact-theme.js && node --check public/blockly-compact-theme/compact-fields.js && node --check public/js/blockly-spike.js",
    "playwright:install": "playwright install chromium",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "dependencies": {
    "blockly": "13.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
